export interface Movie {
    id?: number;           
    name: string;         
    genre: string;        
    relYear: number;   
    rating?: number; 
    imageUrl?: string; 
    description?: string; 
}